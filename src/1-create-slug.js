// Return a URL-friendly "slug": lowercase with hyphens instead of spaces.
// Return null if the title contains banned characters: "!", "#", "?"
const createSlug = (title) => {
  const banCharacters = ['!', '#', '?'];
  return null;
};
// need help I guess you can say I had the first part done but not sure about the other part.
module.exports = {
  createSlug,
};
